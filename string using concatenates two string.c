#include<stdio.h>
    #include<string.h>
    int main(){
        char pushpa[10];
        printf("enter your string\n");
        scanf("%s\n",&pushpa);
    strcat (pushpa,"flower");
        printf("%s\n",pushpa);
        strcat (pushpa,"fire");
        printf("%s",pushpa);
    }
